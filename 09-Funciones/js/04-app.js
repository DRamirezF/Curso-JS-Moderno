const numero1 = 20
const numero2 = '20'

// Una función es, palabraClave seguido de parentesis y puede tener varibales dentro
console.log( parseInt(numero2));    // Función

// Un método es, variable seguido de un punto, palabraClave y despues paréntesis
console.log( numero1.toString() );  // Método