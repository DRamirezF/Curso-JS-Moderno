const autenticado = true;

// if (autenticado ===true) // Forma incorrecta de comparar
if (autenticado) {      // Forma correcta
    console.log('Sí puedes ver Netflix');
}
else {
    console.log('No puedes ver Netflix');
}


// Opreador Ternario
console.log( autenticado ? 'Sí está autenticado' : 'No está autenticado');